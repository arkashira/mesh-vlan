[tool.poetry]
name = "mesh-vlan"
version = "0.1.0"
description = "A clean, responsive web UI for configuring Mesh VLAN."
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.8"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
