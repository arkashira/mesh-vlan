# Mesh Network Project
This project provides a basic implementation of a mesh network in Python.
It includes classes for nodes and the mesh network itself, with methods for adding and removing nodes, monitoring performance, and securing the network.
